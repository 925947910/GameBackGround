<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yxb.cms.dao.OrderMapper">
	<resultMap id="BaseResultMap"
		type="com.yxb.cms.domain.vo.orderInfo">
		<!-- WARNING - @mbggenerated This element is automatically generated by 
			MyBatis Generator, do not modify. This element was generated on Mon Aug 15 
			22:46:30 CST 2016. -->

		<id column="id" property="id" jdbcType="INTEGER" />
		<result column="accountOut" property="accountOut"
			jdbcType="VARCHAR" />
		<result column="accountIn" property="accountIn"
			jdbcType="VARCHAR" />
		<result column="orderRemote" property="orderRemote"
			jdbcType="VARCHAR" />
		<result column="plat" property="plat" jdbcType="INTEGER" />
		<result column="uid" property="uid" jdbcType="INTEGER" />
		<result column="agentId" property="agentId" jdbcType="INTEGER" />
		<result column="cost" property="cost" jdbcType="FLOAT" />
		<result column="currency" property="currency"
			jdbcType="VARCHAR" />
		<result column="coin" property="coin" jdbcType="INTEGER" />
		<result column="orderType" property="orderType"
			jdbcType="INTEGER" />
		<result column="status" property="status" jdbcType="INTEGER" />
		<result column="time" property="time" jdbcType="BIGINT" />
	</resultMap>


	<select id="OrderInfoById" resultMap="BaseResultMap">
		select * from tradeorder
		where id=#{id,jdbcType=INTEGER}
	</select>

	
	<!-- 查询用户总记录数 -->
	<select id="selectCountOrder"
		parameterType="com.yxb.cms.domain.vo.orderInfo" resultType="Long">
		select count(*)from tradeorder
		<include refid="selectPageParmas" />
	</select>
	
		<!-- 查询用户总记录数 -->
	<select id="orderSumOfCost"
		parameterType="com.yxb.cms.domain.vo.orderInfo" resultType="Float">
		select sum(cost)from tradeorder
		<include refid="selectPageParmas" />
	</select>
	
	
	<!-- 用户列表信息分页显示 -->
	<select id="selectOrderInfoListByPage"
		parameterType="com.yxb.cms.domain.vo.orderInfo"
		resultMap="BaseResultMap">
		select * from tradeorder
		<include refid="selectPageParmas" />
		order by time DESC
		LIMIT #{start},#{limit}
	</select>


	<!-- 查询条件 -->
	<sql id="selectPageParmas">
		<where>
			<if
				test="searchTerm != null and searchTerm != '' and searchContent != null and  searchContent != '' and  searchTerm == 'idTerm' ">
				AND id= #{searchContent,jdbcType=INTEGER}
			</if>
			<if
				test="searchTerm != null and searchTerm != '' and searchContent != null and  searchContent != '' and  searchTerm == 'uidTerm' ">
				AND uid= #{searchContent,jdbcType=INTEGER}
			</if>
			<if
				test="searchTerm != null and searchTerm != '' and searchContent != null and  searchContent != '' and  searchTerm == 'orderTypeTerm' ">
				 orderType= #{searchContent,jdbcType=INTEGER}
			</if>
			<if
				test="searchTerm != null and searchTerm != '' and searchContent != null and  searchContent != '' and  searchTerm == 'orderNoTerm' ">
				AND orderRemote= #{searchContent,jdbcType=VARCHAR}
			</if>
			<if
				test="searchTerm != null and searchTerm != '' and searchContent != null and  searchContent != '' and  searchTerm == 'succTerm' ">
			    AND orderType= #{searchContent,jdbcType=INTEGER} AND status=3
			</if>
            <if
				test="searchTerm1 != null and searchTerm1 != '' and searchContent1 != null and  searchContent1 != '' and  searchTerm1 == 'agentIdTerm' ">
				AND agentId= #{searchContent1,jdbcType=INTEGER}
			</if>
			<if
				test="begin != null and begin != '' and end != null and  end != '' ">
				AND time BETWEEN #{begin,jdbcType=BIGINT} and
				#{end,jdbcType=BIGINT}
			</if>
			
		</where>
	</sql>

	<update id="reviewOrder">
		update tradeorder set
		status=#{status,jdbcType=INTEGER}
		where id=#{id,jdbcType=INTEGER} and
		status=2 and orderType=2
	</update>


</mapper>